# Large / regenerable data files — see README.md for how to rebuild these
# from the scripts in scripts/ after downloading the source files.
*.sqlite
*.sqlite3
pp-complete.csv
data/pp-complete.csv

# Power BI dashboard file — built locally from data/processed/price_vs_earnings_by_la_year.csv,
# see docs/power_bi_setup.md for the column type and null-handling setup steps.
*.pbix

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
.vscode/

# Leftover run logs / checkpoints
scripts/load_log.txt
*.log
